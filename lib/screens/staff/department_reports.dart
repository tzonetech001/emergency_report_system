import 'package:flutter/material.dart';

class DepartmentReportsScreen extends StatelessWidget {
  const DepartmentReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Department Reports')),
      body: const Center(child: Text('Department reports placeholder')),
    );
  }
}
